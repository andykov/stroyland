/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "/";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = "./app/js/app.js");
/******/ })
/************************************************************************/
/******/ ({

/***/ "./app/js/app.js":
/*!***********************!*\
  !*** ./app/js/app.js ***!
  \***********************/
/*! no static exports found */
/***/ (function(module, exports) {

function _createForOfIteratorHelper(o, allowArrayLike) { var it; if (typeof Symbol === "undefined" || o[Symbol.iterator] == null) { if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; var F = function F() {}; return { s: F, n: function n() { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }, e: function e(_e) { throw _e; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var normalCompletion = true, didErr = false, err; return { s: function s() { it = o[Symbol.iterator](); }, n: function n() { var step = it.next(); normalCompletion = step.done; return step; }, e: function e(_e2) { didErr = true; err = _e2; }, f: function f() { try { if (!normalCompletion && it["return"] != null) it["return"](); } finally { if (didErr) throw err; } } }; }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

function getSiblings(e) {
  var siblings = [];

  if (!e.parentNode) {
    return siblings;
  }

  var sibling = e.parentNode.firstChild; // collecting siblings

  while (sibling) {
    if (sibling.nodeType === 1 && sibling !== e) {
      siblings.push(sibling);
    }

    sibling = sibling.nextSibling;
  }

  return siblings;
}

function isTouchDevice() {
  return 'ontouchstart' in window || navigator.maxTouchPoints;
}

if (isTouchDevice()) {
  document.querySelector('html').classList.add('is-touch');
} else {
  document.querySelector('html').classList.add('no-touch');
}

tabbis({
  trigger: 'mouseover'
});
var body = document.querySelector('body'),
    nav = document.querySelector('.nav'),
    navTrigger = document.querySelector('.js-nav-trigger'),
    navClose = document.querySelector('.js-nav-close'),
    navBack = document.querySelector('.js-nav-back'),
    navCategory = document.querySelector('.nav-category'),
    navSubCategory = document.querySelector('.nav-subcategory');

function mobileNavClose() {
  var isCategoryShow = navCategory.classList.contains('show');

  if (isCategoryShow) {
    navCategory.classList.remove('show');
    navSubCategory.classList.remove('show');
    navBack.classList.remove('show');
    body.classList.remove('scroll-off');
  }
}

function mobileNavBack() {
  var isSubCategoryShow = navSubCategory.classList.contains('show');

  if (isSubCategoryShow) {
    navSubCategory.classList.remove('show');
    navBack.classList.remove('show');
  }
} // const nav = document.querySelector('.nav');


document.addEventListener('tabbis', function (e) {
  var data = e.detail;
  var siblings = getSiblings(data.tab);
  siblings.forEach(function (item) {
    item.classList.remove('show');
  });
  data.tab.classList.add('show');
  data.pane.parentNode.classList.add('show');
  navBack.classList.add('show');
}, false);
navClose.addEventListener('click', mobileNavClose);
navBack.addEventListener('click', mobileNavBack);

if (isTouchDevice()) {
  navTrigger.addEventListener('click', function () {
    body.classList.toggle('scroll-off');
    this.classList.toggle('active');
    navCategory.classList.toggle('show');
  });
} else {
  navTrigger.addEventListener('mouseenter', navMouseOver, false);
  nav.addEventListener('mouseleave', navMouseOut, false);
}

function navMouseOver(event) {
  if (isTouchDevice()) {
    body.classList.toggle('scroll-off');
  }

  if (navTrigger.contains(event.target)) {
    navCategory.classList.add('show');
  }
}

function navMouseOut(event) {
  if (!navSubCategory.classList.contains('show')) {
    navCategory.classList.remove('show');
  } else {
    navCategory.classList.remove('show');
    navSubCategory.classList.remove('show');
    navBack.classList.remove('show');
    body.classList.remove('scroll-off');
  }
}

var specBtn = document.querySelectorAll('.js-nav-subcategory');

if (specBtn) {
  specBtn.forEach(function (btn) {
    btn.addEventListener('click', function (event) {
      event.stopPropagation();
      event.target.style.display = 'none';
      var stopItem = btn.parentNode.querySelector('[hidden]');
      stopItem.toggleAttribute('hidden');
      this.classList.toggle('active');
    });
  });
}

function isHidden(el) {
  var style = window.getComputedStyle(el);
  return style.display === 'none';
} // Promo slider


var promoSlider = new Splide('.promo .splide', {
  type: 'loop',
  perPage: 2,
  perMove: 1,
  pagination: false,
  autoplay: true,
  breakpoints: {
    1024: {
      arrows: false
    },
    556: {
      perPage: 1,
      arrows: false
    }
  }
});
hideSLiderArrows(promoSlider);
promoSlider.mount(); // Slider banner

var bannerSlider = new Splide('.banner .splide', {
  type: 'loop',
  perPage: 1,
  perMove: 1,
  autoplay: true,
  breakpoints: {
    1024: {
      arrows: false
    }
  }
});
bannerSlider.mount(); // SLider Last News

var lastNewsSliderOptions = {
  type: 'loop',
  perPage: 3,
  perMove: 1,
  pagination: false,
  autoplay: true,
  breakpoints: {
    992: {
      arrows: false
    },
    767: {
      perPage: 2,
      arrows: false
    },
    639: {
      perPage: 1,
      arrows: false
    }
  }
};
var lastNewsSlider = new Splide('.last-news .splide', lastNewsSliderOptions);
hideSLiderArrows(lastNewsSlider);
lastNewsSlider.mount(); // Gallery

var galleryTabs = document.querySelectorAll('[data-gallery-tabs] button');
var galleryItems = document.querySelectorAll('[data-gallery-item]'); // init first gallery

var productGalleryOptions = {
  type: 'loop',
  perPage: 6,
  perMove: 1,
  pagination: false,
  gap: 30,
  autoplay: true,
  breakpoints: {
    1200: {
      perPage: 5
    },
    1024: {
      arrows: true
    },
    992: {
      perPage: 4,
      arrows: false
    },
    767: {
      perPage: 2,
      arrows: false
    },
    639: {
      perPage: 1,
      arrows: false
    }
  }
};
var firstItemGallery = new Splide(galleryItems[0].children[0], productGalleryOptions);
galleryTabs[0].classList.add('active');
hideSLiderArrows(firstItemGallery);
firstItemGallery.mount(); // init gallery on tab click

galleryTabs.forEach(function (tab, i) {
  tab.addEventListener('click', function (e) {
    e.stopPropagation();
    var target = e.target;
    var galleryWrap = galleryItems[i];
    var gallery = galleryWrap.querySelector('.splide');
    var isActive = gallery.classList.contains('is-active');

    if (isHidden(galleryWrap)) {
      galleryWrap.style.display = 'block';
      target.classList.add('active');

      if (!isActive) {
        var g = new Splide(gallery, productGalleryOptions);
        hideSLiderArrows(g);
        g.mount();
      }
    } // remove


    var siblingsTabs = getSiblings(target);
    siblingsTabs.forEach(function (item) {
      item.classList.remove('active');
    }); // hidden prev display gallery

    var siblingsPanes = getSiblings(galleryWrap);
    siblingsPanes.forEach(function (item) {
      item.style.display = 'none';
    });
  });
}); // Product quantity

function incrementValue(e) {
  e.preventDefault();
  var fieldName = e.target.dataset.field;
  var parent = e.target.parentNode;
  var currentVal = parseInt(parent.querySelector('input[name=' + fieldName + ']').value, 10);

  if (!isNaN(currentVal)) {
    parent.querySelector('input[name=' + fieldName + ']').value = currentVal + 1;
  } else {
    parent.querySelector('input[name=' + fieldName + ']').value = 0;
  }
}

function decrementValue(e) {
  e.preventDefault();
  var fieldName = e.target.dataset.field;
  var parent = e.target.parentNode;
  var currentVal = parseInt(parent.querySelector('input[name=' + fieldName + ']').value, 10);

  if (!isNaN(currentVal) && currentVal > 0) {
    parent.querySelector('input[name=' + fieldName + ']').value = currentVal - 1;
  } else {
    parent.querySelector('input[name=' + fieldName + ']').value = 0;
  }
}

var fieldsQuantitys = document.querySelectorAll('.field-quantity');

var _iterator = _createForOfIteratorHelper(fieldsQuantitys),
    _step;

try {
  for (_iterator.s(); !(_step = _iterator.n()).done;) {
    var field = _step.value;
    field.addEventListener('click', function (event) {
      if (event.target.className === 'field-quantity__plus') {
        incrementValue(event);
      }

      if (event.target.className === 'field-quantity__minus') {
        decrementValue(event);
      }
    });
  }
} catch (err) {
  _iterator.e(err);
} finally {
  _iterator.f();
}

document.addEventListener('click', function (event) {
  // check click outside menu
  if (!isOutsideClick('.header__top-panel', event.target)) {
    return false;
  }

  if (isOutsideClick('.nav', event.target)) {
    navCategory.classList.remove('show');
    navSubCategory.classList.remove('show');
    navBack.classList.remove('show');
    body.classList.remove('scroll-off');
    menuClose();
  }
});

function isOutsideClick(ignor, target) {
  var ignoreElement = document.querySelector(ignor);
  return !ignoreElement.contains(target);
}

function hideSLiderArrows(slider) {
  slider.on('mounted', function () {
    var arrows = slider.root.querySelector('.splide__arrows');

    if (arrows && slider.length <= slider.options.perPage) {
      arrows.style.display = 'none';
    }
  });
} //


function overlayVisible(display) {
  var overlay = document.querySelector('.overlay');

  if (!overlay) {
    return false;
  }

  if (display === true) {
    overlay.classList.add('show');
  } else {
    overlay.classList.remove('show');
  }
} // menu mobile


var menuTrigger = document.querySelector('.js-menu-trigger'),
    mobileMenu = document.querySelector('[data-mobile-menu]'),
    mobileSubMenuTrigger = document.querySelectorAll('[data-mobile-submenu-trigger]'),
    mobileMenuBack = document.querySelector('.js-menu-back'),
    mobileMenuClose = document.querySelector('.js-menu-close');
menuTrigger.addEventListener('click', menuShow, false);
mobileMenuBack.addEventListener('click', subMenuBack, false);
mobileMenuClose.addEventListener('click', menuClose, false);
mobileSubMenuTrigger.forEach(function (trigger) {
  trigger.addEventListener('click', subMenuShow, false);
});

function menuShow(e) {
  if (!mobileMenu.classList.contains('show')) {
    mobileMenu.classList.add('show');
    body.classList.add('scroll-off');
    overlayVisible(true);
  }
}

function subMenuShow(e) {
  var target = e.target,
      parent = target.parentNode;
  e.preventDefault();
  var siblings = getSiblings(parent);
  siblings.forEach(function (item) {
    item.classList.remove('show');
  });

  if (!parent.classList.contains('show')) {
    parent.classList.add('show');
    mobileMenuBack.classList.add('show');
  }
}

function subMenuBack(e) {
  mobileSubMenuTrigger.forEach(function (trigger) {
    if (trigger.classList.contains('show')) {
      trigger.classList.remove('show');
    }
  });
  mobileMenuBack.classList.remove('show');
}

function menuClose() {
  subMenuBack();
  mobileMenu.classList.remove('show');
  mobileMenuBack.classList.remove('show');
  body.classList.remove('scroll-off');
  overlayVisible(false);
}

var mobileContactsTrigger = document.querySelector('.js-contacts-trigger');

if (mobileContactsTrigger) {
  mobileContactsTrigger.addEventListener('click', function (e) {
    var contacts = document.querySelector('[data-mobile-contacts]');

    if (!contacts) {
      return false;
    }

    e.target.classList.toggle('active');
    contacts.classList.toggle('show');
  });
} // Footer Nav Collapse


var collapsibleTriggers = document.querySelectorAll('[data-collapsible]');

if (collapsibleTriggers.length) {
  collapsibleTriggers.forEach(function (trigger) {
    trigger.addEventListener('click', function () {
      this.classList.toggle('active');
      var collapsibleBody = this.parentNode.querySelector('[data-collapsible-body]');

      if (collapsibleBody.style.maxHeight) {
        collapsibleBody.style.maxHeight = null;
      } else {
        collapsibleBody.style.maxHeight = collapsibleBody.scrollHeight + 'px';
      }
    });
  });
}

/***/ })

/******/ });
//# sourceMappingURL=main.js.map